# 2025-09-29

## ✅ Fixes & Enhancements
- [x] Ordenamos las etiquetas del panel de detalle para evitar parpadeos cada vez que se mueve el cursor.
- [x] Añadimos spinner y feedback optimista en `start/stop/restart/remove` para que las acciones respondan al instante.
- [x] Simplificamos el refresco: ahora el selector persiste tras actualizar y el mensaje vacío indica cómo ver todos los contenedores con `a`.

## ⚠️ Observaciones
- [ ] Revisar rendimiento del refresco cuando hay decenas de contenedores (posible batch o throttle).
- [ ] Confirmar que los estados `paused` y `dead` se muestran adecuadamente al alternar entre "running" y "all".

## 🔜 Próximos objetivos (según TODO.md)
- [ ] Panel de detalles enriquecido (montajes, env vars, healthcheck).
- [ ] Barra de búsqueda y filtros rápidos en la TUI.
- [ ] Carga de configuración desde YAML/env (`internal/config.Load`).
- [ ] Acciones de imágenes (`images rm`/`prune`) con confirmación en CLI/TUI.
- [ ] Vistas dedicadas para Images / Volumes / Networks / Stacks.

## 📝 Notas rápidas
- Mantener la coherencia visual del header; colores actualizados: fondo 60, texto 231.
- Cuando la lista está vacía mostramos un CTA claro para usar `a`; validar copy con feedback de usuarios.
